import { SocialLoginCallback } from '@/features/auth';

export default function AuthCallbackPage() {
  return (
    <main className="flex min-h-dvh items-center justify-center">
      <SocialLoginCallback />
    </main>
  );
}
