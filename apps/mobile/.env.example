# 백엔드 API 주소
EXPO_PUBLIC_API_BASE_URL=https://chapchap.kr/api

# WebView가 불러올 웹 주소 (개발 빌드 전용)
# 실기기에서는 localhost가 기기 자신을 가리키므로 개발 PC의 LAN 주소를 사용한다.
# apps/web에서 `pnpm exec vite --host` 실행 시 출력되는 Network 주소를 그대로 넣는다.
# preview·production 빌드는 이 값 대신 eas.json에 지정한 배포 주소를 사용한다.
EXPO_PUBLIC_WEB_URL=
