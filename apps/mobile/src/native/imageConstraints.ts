/** 백엔드가 받는 이미지의 최대 변 길이(px). 가로·세로 중 큰 쪽 기준이다. 영수증·프로필 이미지에 공통 적용된다. */
export const MAX_IMAGE_DIMENSION = 4096;
/** 백엔드가 받는 이미지의 최대 파일 크기(byte). 영수증·프로필 이미지에 공통 적용된다. */
export const MAX_IMAGE_FILE_SIZE_BYTES = 5 * 1024 * 1024;
