/**
 * 디자인 시안의 모서리 반경 토큰.
 *
 * 시안에 이름이 정해져 있지 않아 값을 그대로 이름에 쓴다.
 *
 * NOTE: 30과 32는 2px 차이지만 각각 다른 용도로 쓰는 값이므로 합치지 않는다.
 * NOTE: 웹은 `apps/web/src/app/styles/radius.css`에 같은 값이 있으므로 함께 수정해야 한다.
 */
export const RADIUS = {
  '05': 5,
  '08': 8,
  '15': 15,
  '16': 16,
  /** 네비게이션 탭 */
  '30': 30,
  /** 기본값으로 가장 많이 사용 */
  '32': 32,
  '46': 46,
} as const;
