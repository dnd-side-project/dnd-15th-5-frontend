/** 영수증 네이티브 화면에서 뒤로 가기 버튼과 상단 Safe Area 사이의 간격. */
export const RECEIPT_BACK_BUTTON_SAFE_AREA_OFFSET = 18;

/** 스캔 애니메이션이 인지될 수 있도록 보장하는 최소 노출 시간. */
export const MINIMUM_RECEIPT_SCAN_DURATION_MS = 2000;
